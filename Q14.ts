 //Q14 Transform an array of words into a new array with the lengths of each word. The map function might be useful here.
 
  const words: string[] = ["apple", "banana", "orange"];
const wordLengths: number[] = words.map((word) => word.length);
console.log(wordLengths);





